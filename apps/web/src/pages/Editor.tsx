/**
 * Mode Animateur — création/édition de deck.
 * Implémenté à l'étape 6 du plan de build (voir SPEC.md §9).
 */
export default function Editor() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-12">
      <h1 className="text-2xl font-bold">Créer un atelier</h1>
      <p className="mt-2 text-encre/80">
        L'éditeur de deck arrive bientôt.
      </p>
    </main>
  );
}
